// Public API of the notes feature. Grow this as other parts of the app
// (e.g. the dashboard) need to consume notes pieces, instead of importing
// from internal paths.
export { RecentNotes } from './components/recent-notes';
